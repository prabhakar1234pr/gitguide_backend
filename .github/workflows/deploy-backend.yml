name: Deploy Backend to Render

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python 3.12
      uses: actions/setup-python@v4
      with:
        python-version: "3.12"
    
    - name: Cache pip dependencies
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Test backend imports and syntax
      env:
        # Use mock values for testing imports (real DB will be used in deployment)
        DATABASE_URL: postgresql+asyncpg://mock:mock@localhost:5432/mock
        AZURE_OPENAI_KEY: mock_key_for_testing
        AZURE_OPENAI_ENDPOINT: https://mock.openai.azure.com/
        AZURE_OPENAI_API_VERSION: 2024-04-01-preview
        AZURE_OPENAI_DEPLOYMENT_GPT_4_1: mock_deployment
        GITHUB_ACCESS_TOKEN: ghp_mock_token_for_testing
        CLERK_SECRET_KEY: sk_mock_clerk_key_for_testing
        GROQ_API_KEY: mock_groq_key
        OPENAI_API_KEY: sk-mock_openai_key
      run: |
        # Test imports and syntax (no actual DB connection needed)
        python -c "from app.api_server import app; print('✅ FastAPI app imports successfully')"
        python -c "from app.database_models import Base; print('✅ Database models import successfully')" 
        python -c "from agent.agent_orchestrator import GitGuideAgent; print('✅ AI Agent imports successfully')"
        python -c "from app.routes.health_endpoints import router; print('✅ Health endpoints import successfully')"
        python -c "from app.routes.project_endpoints import router; print('✅ Project endpoints import successfully')"
        echo "✅ All backend components import successfully!"

  deploy:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Deploy to Render
      uses: johnbeynon/render-deploy-action@v0.0.8
      with:
        service-id: ${{ secrets.RENDER_SERVICE_ID }}
        api-key: ${{ secrets.RENDER_API_KEY }}
        
    - name: Wait for deployment to stabilize
      run: sleep 90
      
    - name: Test deployed backend with debugging
      run: |
        echo "🔍 Testing backend deployment at: ${{ secrets.BACKEND_URL }}"
        
        # Test root endpoint first
        echo "📍 Testing root endpoint..."
        curl -v -f ${{ secrets.BACKEND_URL }}/ || echo "❌ Root endpoint failed"
        
        # Test health endpoint
        echo "🏥 Testing health endpoint..."
        curl -v -f ${{ secrets.BACKEND_URL }}/ping || echo "❌ Health endpoint failed"
        
        # Test with longer timeout and better error reporting
        echo "🔍 Detailed health check with verbose output..."
        curl -v --max-time 30 ${{ secrets.BACKEND_URL }}/ping || {
          echo "❌ Backend health check failed!"
          echo "🔍 Attempting to get more info..."
          curl -I ${{ secrets.BACKEND_URL }}/ || echo "❌ Even root endpoint is unreachable"
          exit 1
        }
        
        echo "✅ Backend deployment successful!"
        
        # Test API documentation (optional)
        curl -f ${{ secrets.BACKEND_URL }}/docs || echo "⚠️ Docs endpoint check failed (may not be critical)"
        
        echo "🎉 Backend deployment and health checks completed!"
